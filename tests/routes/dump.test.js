/* const request = require('supertest');
const helpers = require('../helpers');
const app = helpers.app();

describe('Test the init route dumper-columns', () => {
  test('It should return status 200 on the main route', async () => {
    const response = await request(app).get('/dump/columns');
    expect(response.calledOnce).toBe(true);
  });
});
 */